// Package {{.Snake}} is the {{.Snake}} module's assembly.
package {{.Snake}}

import (
	"github.com/samber/do/v2"

	"{{.Module}}/internal/pkg/registry"
	"{{.Module}}/internal/{{.Snake}}/biz"
	"{{.Module}}/internal/{{.Snake}}/data"
	"{{.Module}}/internal/{{.Snake}}/service"
)

var Package = do.Package(
	do.Lazy(data.New{{.Pascal}}Repo),
	registry.Lazy(biz.New{{.Pascal}}Usecase),
	registry.Lazy2(service.New{{.Pascal}}Service),
	do.LazyNamed(registry.RoutePrefix+"{{.Snake}}", service.{{.Pascal}}Routes),
)
