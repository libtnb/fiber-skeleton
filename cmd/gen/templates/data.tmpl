package data

import (
	"context"

	"github.com/go-rio/rio"
	"github.com/samber/do/v2"

	"{{.Module}}/internal/{{.Snake}}/biz"
)

type {{.Camel}}Repo struct {
	db *rio.DB
}

func New{{.Pascal}}Repo(i do.Injector) (biz.{{.Pascal}}Repo, error) {
	return &{{.Camel}}Repo{
		db: do.MustInvoke[*rio.DB](i),
	}, nil
}

func (r *{{.Camel}}Repo) List(ctx context.Context, page, limit uint) ([]*biz.{{.Pascal}}, int64, error) {
	total, err := rio.From[biz.{{.Pascal}}]().Count(ctx, r.db)
	if err != nil {
		return nil, 0, err
	}

	list, err := rio.From[biz.{{.Pascal}}]().
		Offset(int((page - 1) * limit)).
		Limit(int(limit)).
		OrderBy("id").
		All(ctx, r.db)
	if err != nil {
		return nil, 0, err
	}

	{{.Camel}}s := make([]*biz.{{.Pascal}}, len(list))
	for i := range list {
		{{.Camel}}s[i] = &list[i]
	}

	return {{.Camel}}s, total, nil
}

func (r *{{.Camel}}Repo) Get(ctx context.Context, id uint) (*biz.{{.Pascal}}, error) {
	{{.Camel}}, err := rio.Find[biz.{{.Pascal}}](ctx, r.db, id)
	if err != nil {
		return nil, err
	}

	return {{.Camel}}, nil
}

func (r *{{.Camel}}Repo) Create(ctx context.Context, {{.Camel}} *biz.{{.Pascal}}) error {
	return rio.Insert(ctx, r.db, {{.Camel}})
}

// Update is read-modify-write: it keeps CreatedAt intact and reports a
// missing row instead of upserting.
func (r *{{.Camel}}Repo) Update(ctx context.Context, {{.Camel}} *biz.{{.Pascal}}) (*biz.{{.Pascal}}, error) {
	existing, err := rio.Find[biz.{{.Pascal}}](ctx, r.db, {{.Camel}}.ID)
	if err != nil {
		return nil, err
	}

	existing.Name = {{.Camel}}.Name
	if err := rio.Update(ctx, r.db, existing); err != nil {
		return nil, err
	}

	return existing, nil
}

func (r *{{.Camel}}Repo) Delete(ctx context.Context, id uint) error {
	n, err := rio.From[biz.{{.Pascal}}]().Where("id = ?", id).DeleteAll(ctx, r.db)
	if err != nil {
		return err
	}
	if n == 0 {
		return rio.ErrNotFound
	}

	return nil
}
