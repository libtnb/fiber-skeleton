package request

type {{.Pascal}}ID struct {
	ID uint `uri:"id" validate:"required && number"`
}

type {{.Pascal}}Add struct {
	Name string `json:"name" form:"name" validate:"required && notblank && min:3 && max:255"`
}

type {{.Pascal}}Update struct {
	ID   uint   `uri:"id" validate:"required && number"`
	Name string `json:"name" form:"name" validate:"required && notblank && min:3 && max:255"`
}
