version: "2"

linters:
  default: standard
  exclusions:
    generated: lax
    paths:
      - mocks

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/libtnb/fiber-skeleton
  exclusions:
    paths:
      - mocks
